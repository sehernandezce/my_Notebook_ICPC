
// Algorithm or Logic: Permutation
// Complexity: ?
// Permutation through indices
// Following permutation lexicographically.

void permut(ll n){
    for(int i=0; i<n; i++) arr_i[i]=i; // Create Indexes
    
    do{
        //process permutation
  
    }while(next_permutation(arr_i,arr_i+n)); // next_permutation(vec.begin(),vec.end());
}